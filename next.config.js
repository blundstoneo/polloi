/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['avatars.githubusercontent.com', 'github.com', 'avatars.githubusercontent.com'],
  },
}

module.exports = nextConfig
